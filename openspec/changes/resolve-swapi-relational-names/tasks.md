## 1. Resolución Asíncrona de Nombres Relacionales en RightPane

- [x] 1.1 Implementar un mapa de nombres resueltos `resolvedNames` y resolución asíncrona con `useEffect` en `RightPane.tsx`.
- [x] 1.2 Actualizar el renderizado de enlaces relacionales para mostrar los nombres reales (ej: `[Tatooine]`, `[A New Hope]`) con fallback a `[CARGANDO...]`.

## 2. Pruebas y Verificación

- [x] 2.1 Actualizar `src/components/RightPane.test.tsx` para verificar la resolución y visualización de los nombres reales en lugar de etiquetas genéricas.
- [x] 2.2 Ejecutar `npx vitest run` y `npx tsc --noEmit` para asegurar que el 100% de los tests sigan pasando.
